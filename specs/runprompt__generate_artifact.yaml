apiVersion: v1
tool:
  name: runprompt__generate_artifact
  description: |-
    /**
     * Generate one file with runprompt and save it under MCP_SHELL_SPEC_DIR.
     * @param artifact_type Artifact type: script, mcp-shell-yaml, or runprompt-prompt.
     * @param requirements Natural language requirements used to generate file content.
     */
  input:
    properties:
      artifact_type:
        type: string
        description: "Artifact type: script, mcp-shell-yaml, or runprompt-prompt."
      requirements:
        type: string
        description: Natural language requirements for the generated file.
    required: [artifact_type, requirements]
  output:
    type: object
    properties:
      status: { type: string }
      exit_code: { type: number }
      stdout: { type: string }
      stderr: { type: string }
      command: { type: string }
      execution_time_ms: { type: number }
execution:
  shell:
    mode: direct
  timeoutMs: 120000
  maxOutputBytes: 1048576
  script:
    path: ./scripts/runprompt_generate_artifact.sh
    interpreter: bash
    args:
      - "{{artifact_type}}"
      - "{{requirements}}"
